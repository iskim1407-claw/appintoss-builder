# 앱인토스 빌더 런칭 — 플랫폼별 최종 포스트

> 작성일: 2026-02-15
> 상태: 복붙 준비 완료

---

## ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 1. 디스콰이어 (disquiet.io) — 메이커로그
## ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

**카테고리:** 메이커로그
**포맷:** 마크다운 지원, 이미지 첨부 가능

### 제목
```
토스 미니앱을 드래그앤드롭으로 만드는 노코드 빌더를 만들었습니다
```

### 본문
```
안녕하세요, 사이드 프로젝트 하나 공유드립니다.

**앱인토스 빌더**는 토스 미니앱(앱인토스) UI를 드래그앤드롭으로 만들 수 있는 웹 기반 노코드 빌더입니다.

## 만든 배경

토스 앱인토스 생태계가 열리면서 미니앱을 만들어보고 싶은 분들이 많아졌는데요. 막상 시작하려면 TDS(Toss Design System) 컴포넌트를 하나하나 코드로 짜야 하고, SDK 연동도 해야 하고, 심사 요구사항도 맞춰야 합니다. 비개발자는 물론이고, 개발자도 프로토타이핑에 시간이 꽤 걸리더라고요.

"토스 미니앱용 Figma-to-Code 같은 게 있으면 좋겠다"는 생각에서 시작했습니다.

## 현재 기능

- **34종 TDS 기반 컴포넌트**: 네비게이션, 버튼, 탭바, 입력 필드, 다이얼로그 등 토스 디자인 시스템을 따르는 UI 컴포넌트를 드래그앤드롭으로 배치
- **핀테크 특화 컴포넌트**: 결제, 계좌, 신용점수, 거래내역 등 금융 서비스에서 자주 쓰는 UI 포함
- **퀴즈/테스트 컴포넌트**: 인트로 → 질문 → 결과 흐름을 빠르게 구성
- **SDK Export**: 만든 UI를 토스 미니앱용 Next.js 프로젝트로 내보내기
- **Toss 심사 모드**: Safe-area, HTTPS 등 심사 요구사항 자동 적용
- **실시간 미리보기**: 모바일 프레임에서 바로 확인

## 기술 스택

Craft.js (드래그앤드롭 에디터) + Next.js 14 + Zustand + Vercel 배포

## 링크

- 🔗 데모: https://appintoss-builder.vercel.app
- 💻 GitHub: https://github.com/iskim1407-claw/appintoss-builder

## 솔직한 현재 상태

아직 MVP 단계입니다. Export된 코드의 완성도나 실제 심사 통과율 같은 건 더 검증이 필요합니다. 컴포넌트도 실제 TDS 라이브러리를 래핑한 게 아니라 스타일을 맞춘 커스텀 구현입니다.

토스 미니앱에 관심 있으신 분들, 노코드 빌더 UX에 의견 있으신 분들, 어떤 피드백이든 환영합니다. 특히 "이 기능이 빠지면 안 된다" 같은 의견 주시면 우선순위에 바로 반영하겠습니다. 🙏
```

---

## ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 2. GeekNews (news.hada.io) — Show
## ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

**카테고리:** Show
**포맷:** 제목 + URL + 본문(plain text, 간결하게). 마크다운 미지원.
**참고:** GeekNews Show는 HN "Show HN" 스타일. 제목에 "Show GN:" 접두사, 본문은 짧게.

### 제목
```
Show GN: 앱인토스(토스 미니앱) 노코드 빌더
```

### URL
```
https://appintoss-builder.vercel.app
```

### 본문
```
토스 미니앱(앱인토스) UI를 드래그앤드롭으로 만들 수 있는 웹 빌더입니다.

- Craft.js 기반 비주얼 에디터
- TDS 스타일 컴포넌트 34종 (네비게이션, 버튼, 탭바, 핀테크 UI 등)
- 만든 UI를 Next.js 프로젝트로 Export
- Toss 심사 모드 (safe-area, SDK 브릿지 자동 적용)

기술 스택: Next.js 14 + Craft.js + Zustand + Vercel

아직 MVP 단계이고, Export 코드 완성도는 개선 중입니다. 토스 미니앱 생태계에서 프로토타이핑 속도를 높이는 게 목표입니다.

GitHub: https://github.com/iskim1407-claw/appintoss-builder
```

---

## ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
## 3. velog — 블로그 글
## ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

**포맷:** 풀 마크다운 지원, 태그 별도 입력

### 제목
```
앱인토스 노코드 빌더를 만든 이유 — Craft.js로 토스 미니앱 빌더 개발기
```

### 태그
```
토스, 앱인토스, 노코드, Craft.js, Next.js, 사이드프로젝트, 핀테크, TDS
```

### 본문
```markdown
## 왜 만들었나

토스 앱인토스 생태계가 점점 커지고 있습니다. 미니앱을 만들어서 토스 유저 2천만 명에게 서비스를 노출할 수 있다는 건 매력적이죠. 하지만 실제로 앱인토스 미니앱 만들기를 시작하면, 생각보다 진입장벽이 있습니다.

- TDS(Toss Design System) 컴포넌트를 직접 구현하거나 래핑해야 함
- 토스 SDK 연동 (인증, 결제, 브릿지 등)
- 심사 요구사항 충족 (Safe-area, HTTPS, 보안 점검)
- 모바일 WebView 환경 최적화

개발자라면 하나씩 해결할 수 있지만, 프로토타이핑 단계에서 이걸 다 하기엔 시간이 아깝습니다. 비개발자라면 시작조차 어렵고요.

"토스 미니앱 노코드 빌더가 있으면 최소한 UI 프로토타입은 빠르게 만들 수 있지 않을까?" — 이게 시작이었습니다.

## 기술적 도전

### Craft.js 선택

드래그앤드롭 에디터를 직접 만들면 DOM 조작, 직렬화, Undo/Redo 등 삽질이 끝이 없습니다. Craft.js는 React 컴포넌트를 그대로 에디터 노드로 쓸 수 있어서, TDS 스타일 컴포넌트를 만들고 바로 에디터에 등록하는 구조가 깔끔했습니다.

다만 Craft.js가 활발히 유지보수되는 프로젝트는 아니라서, 모바일 드래그 지원(mobile-drag-drop 폴리필), 레이어 패널 커스터마이징 등에서 직접 해결해야 할 부분이 있었습니다.

### 34종 컴포넌트 설계

TDS를 공식 라이브러리로 쓸 수 없는 상황이라 (npm 미공개), 토스 앱의 실제 UI를 참고해서 커스텀 구현했습니다. 카테고리별로:

| 카테고리 | 개수 | 예시 |
|---------|------|------|
| TDS 핵심 | 7종 | 네비게이션, 버튼, 텍스트, 뱃지, 리스트 항목, 탭, 탭바 |
| TDS 입력 | 3종 | 입력 필드, 스위치, 체크박스 |
| TDS 피드백 | 4종 | 진행바, 토스트, 스켈레톤, 다이얼로그 |
| TDS 레이아웃 | 2종 | 하단 CTA, 바텀시트 |
| 기본 컴포넌트 | 9종 | 이미지, 카드, 리스트, 구분선, 헤더 등 |
| 핀테크 | 5종 | 결제, 계좌, 신용점수, 상품 비교, 거래내역 |
| 퀴즈/테스트 | 3종 | 인트로, 질문, 결과 |

각 컴포넌트마다 Craft.js의 `craft` 설정(기본 props, 설정 패널, 드래그 규칙)을 정의해야 해서, 컴포넌트 하나당 작업량이 꽤 됩니다.

### Export 구조

빌더에서 만든 UI를 실제로 쓰려면 코드로 내보내야 합니다. Craft.js의 직렬화된 노드 트리를 순회하면서 Next.js 페이지 코드 + 컴포넌트 파일 + 토스 SDK 설정을 포함한 프로젝트를 JSZip으로 패키징합니다. 이 부분이 가장 도전적이었고, 솔직히 아직 완성도가 부족합니다.

### 심사 모드

토스 미니앱 심사에서 자주 지적되는 항목들 — Safe-area 미적용, HTTPS 미사용, SDK 브릿지 누락 등 — 을 빌더 단계에서 자동으로 적용해주는 모드를 만들었습니다. 아직은 기본적인 수준이고, 실제 심사를 통과한 사례가 있는 건 아닙니다.

## 현재 상태와 한계

솔직하게 말하면:

- Export된 코드가 바로 프로덕션에 쓸 수 있는 수준은 아닙니다
- TDS 공식 라이브러리가 아니라 룩앤필만 맞춘 커스텀 구현입니다
- 복잡한 비즈니스 로직은 빌더로 처리할 수 없습니다
- 실제 심사 통과 사례는 아직 없습니다

하지만 "아이디어 → 프로토타입" 단계에서 속도를 높여주는 도구로는 쓸 수 있다고 생각합니다. 특히 앱인토스 미니앱 만들기를 처음 시도하는 분들이 "이런 느낌으로 만들면 되는구나"를 빠르게 확인하는 용도로요.

## 링크

- 🔗 데모: [appintoss-builder.vercel.app](https://appintoss-builder.vercel.app)
- 💻 GitHub: [github.com/iskim1407-claw/appintoss-builder](https://github.com/iskim1407-claw/appintoss-builder)

피드백이나 기여 환영합니다. 🙏
```

---

## 📋 포스팅 체크리스트

- [ ] 디스콰이어: https://disquiet.io → 메이커로그 → 새 글 작성
- [ ] GeekNews: https://news.hada.io → Show → 새 글 작성
- [ ] velog: https://velog.io → 새 글 작성
- [ ] 각 플랫폼 포스팅 후 URL 기록
